<div class = "center jumbotron">
    <div class = "text-center">
        <h1>Knowhowpocket</h1>
        <% if logged_in? %>
        <p>ログイン完了ユーザ： <%= current_user.name %></p>
        
        <% else %>
            
        <%= link_to "Sign up now", signup_path, class: "btn btn-lg btn-primary" %>
        <%= link_to "Login", login_path, class: "btn btn-lg btn-danger" %>
        
        <% end %>
    </div>
</div>